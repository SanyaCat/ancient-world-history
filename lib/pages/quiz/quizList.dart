import 'package:flutter/material.dart';

class QuizList extends StatelessWidget {
  @override
  build(context) {
    return Container(
      child: Scaffold(
        body: Text("Quiz List"),
      ),
    );
  }
}